// functions.js

function helloWorld() {
  alert("Hello, World!");
}


function addIfLivesInCity(person) {}

function filterPeopleByCity() {

  let city = "Bern";
  let cities = []; // list of <span>
  let citizens = []; // list of <figure>

  cities = document.getElementsByClassName("city");
  cities.forEach(findCitizens()){
// citizens <-- figures with people from that city
    
  }
  
}

function displayPersonDetails() {
  // person = who's the person (object)?
  //  get the img's id
  //  look for the id in the people list
  //  return the object
  // details_str = person.details();
  // format(details_str)
  // display tooltip
  return true;
}

